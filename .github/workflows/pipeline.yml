name: pipeline
on: 
  workflow_dispatch

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: install java
        run: | 
          sudo apt-get update
          sudo apt-install java
          sudo apt-install maven
        
  
  
